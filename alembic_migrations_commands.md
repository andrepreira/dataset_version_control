alembic init <name_directory>

alembic current

alembic revision -m <name_directory>

- criando requirements.txt

pip freeze > requirements.txt

alembic history --verbose

alembic upgrade  <code>

alembic downgrade <code>